# RSA-Network-Security-Project
This project implements RSA encryption and decryption in C. It generates public and private keys, reads plaintext from a file, encrypts it using the private key, and decrypts it using the public key. It demonstrates basic concepts of public key cryptography in network security.
